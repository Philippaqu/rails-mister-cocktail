<h2>Mister Cocktail</h2>
<%= link_to 'Add a new cocktail', new_cocktail_path, mehtod: :post, class: 'btn btn-primary'%>



<ul>
  <% @cocktails.each do |cocktail| %>
  <li><%= cocktail.name %>
    <%= link_to 'Show details', cocktail_path(cocktail) %>
    <%= link_to 'Delete this cocktail', cocktail_path(cocktail), method: :delete %>
  </li>
<% end %>
</ul>
